#include <stdio.h>

/**
 * main - print lowercase alphabet in reverse
 *
 * Return: Always 0
 */
int main(void)
{
	char bt;

	for (bt = 'z'; le >= 'a'; le--)
		putchar(le);

	putchar ('\n');

	return (0);
}
